{
  "watch": ["src", "../api/dist"],
  "ext": "ts",
  "ignore": [],
  "exec": "npm run build && npm link && homebridge -I -D",
  "signal": "SIGTERM",
  "env": {
    "NODE_OPTIONS": "--trace-warnings"
  }
}

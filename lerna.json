{
  "$schema": "node_modules/lerna/schemas/lerna-schema.json",
  "version": "0.0.3",
  "packages": ["packages/*"],
  "command": {
    "version": {
      "allowBranch": ["main"],
      "message": "chore(release): publish %s"
    }
  }
}
